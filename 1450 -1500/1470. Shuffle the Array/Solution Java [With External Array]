class Solution {
    public int[] shuffle(int[] nums, int n) {
        int ans[] = new int[n*2];
        int arr_pos = 0;
        for(int i = 0;i< n;i++){
            ans[arr_pos++] = nums[i];
            ans[arr_pos++] = nums[i+n];
        }
        return ans;        
    }
}
